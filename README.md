# Campus Treasure Lite (校园宝 - 演示版)

一个基于 Next.js 和 React Native (Expo) 构建的校园二手交易平台演示项目。本项目专为展示核心交易流程而设计，精简了复杂的商业功能，专注于提供流畅的用户体验。

## 📱 项目定位

**核心场景**: 校园内的闲置物品流转。
**主要功能**:

- 🔐 **用户认证**: 手机号注册/登录，个人信息管理。
- 📦 **物品交易**: 发布闲置物品（图文），浏览首页信息流，搜索与筛选。
- 🛒 **订单闭环**: 下单购买，模拟支付，订单状态管理（待支付/待发货/已完成）。
- 💬 **即时通讯**: 买卖双方简单的文字聊天。

## 🛠️ 技术栈

- **Monorepo**: pnpm workspaces
- **后端**: Next.js 16 (App Router), Prisma, MongoDB, Redis, Socket.io
- **移动端**: React Native (Expo), NativeWind (Tailwind CSS), Zustand
- **语言**: TypeScript

## 🚀 快速开始

### 1. 环境要求

- Node.js >= 18
- pnpm >= 9
- Docker & Docker Compose (用于启动数据库)

### 2. 启动基础设施

在项目根目录运行：

```bash
# 启动 MongoDB 和 Redis
docker-compose -f docker/docker-compose.yml up -d
```

### 3. 安装依赖

```bash
pnpm install
```

### 4. 启动开发服务器

```bash
# 同时启动后端和移动端
npm run dev

# 或者分别启动
# 后端 (http://localhost:3000)
pnpm --filter @campus-treasure/backend dev

# 移动端 (Expo Go)
pnpm --filter @campus-treasure/mobile dev
```

## 📂 项目结构

```
.
├── apps
│   ├── backend    # Next.js 后端 API 服务
│   └── mobile     # Expo 移动端应用
├── packages
│   ├── shared-types # 前后端共享类型定义
│   └── shared-utils # 共享工具函数
└── docker         # Docker Compose 配置
```

## ✅ 已完成功能

- [x] Monorepo 基础架构搭建
- [x] 手机号注册与密码登录 (JWT)
- [x] 用户个人信息管理
- [x] 移动端 UI 框架搭建
- [x] 数据库模型定义 (User, Item, Order)
- [x] 认证中间件与 API 路由

## 📅 待开发计划

1. **移动端对接**: 实现 Token 存储与请求拦截。
2. **物品模块**: 实现物品发布与首页流展示。
3. **交易模块**: 完成下单与模拟支付流程。
